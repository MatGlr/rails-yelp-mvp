<ul>
  <%= form_for [@restaurant, @review] do |f| %>
  <p><%= f.label :content %>
    <%= f.text_area :content %></p>
    <p><%= f.label :rating %>
      <%= f.number_field :rating %></p>
      <%= f.submit "Add a review" %>
      <% end %>
    </ul>
